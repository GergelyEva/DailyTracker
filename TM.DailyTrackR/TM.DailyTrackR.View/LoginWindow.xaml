<Window x:Class="TM.DailyTrackR.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LoginWindow" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" Background="LightGray">
    <Window.Resources>
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="DarkOliveGreen"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="DarkOliveGreen"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="DarkGreen"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Welcome to DailyTrackR" FontSize="24" FontWeight="Bold" Foreground="DarkOliveGreen" Margin="0,0,0,20"/>
            <TextBox Name="UsernameTextBox" Style="{StaticResource TextBoxStyle}" Width="200" Height="30"/>
            <PasswordBox Name="PasswordBox" Style="{StaticResource PasswordBoxStyle}" Width="200" Height="30" Margin="0,10"/>
            <Button Content="Login" Style="{StaticResource ButtonStyle}" Width="100" Height="35" Margin="0,20,0,0" Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
